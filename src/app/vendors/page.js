import React from 'react';
import Vendors from '@/components/Vendor';

function VendorsPage() {
  return <Vendors />;  // ✅ Capitalized, so React treats it as a component
}

export default VendorsPage;
